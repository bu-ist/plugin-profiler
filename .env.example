PORT=9000
PLUGIN_PATH=./my-plugin

# ── LLM Provider ──────────────────────────────────────────────────────────────
# Choose one provider and set the corresponding API key.
# Run with: plugin-profiler analyze ./my-plugin --llm <provider> --api-key <key>
#
# Claude (Anthropic)  https://console.anthropic.com/
LLM_PROVIDER=claude
LLM_MODEL=claude-haiku-4-5-20251001
LLM_API_KEY=sk-ant-...
#
# ChatGPT (OpenAI)    https://platform.openai.com/api-keys
# LLM_PROVIDER=openai
# LLM_MODEL=gpt-4o-mini
# LLM_API_KEY=sk-...
#
# Gemini (Google)     https://aistudio.google.com/apikey
# LLM_PROVIDER=gemini
# LLM_MODEL=gemini-2.0-flash
# LLM_API_KEY=...
#
# Ollama (local, free) — requires --profile llm and ~4 GB model download
# LLM_PROVIDER=ollama
# LLM_MODEL=qwen2.5-coder:7b

# ── Advanced ──────────────────────────────────────────────────────────────────
LLM_API_BASE_URL=
OLLAMA_HOST=http://ollama:11434
LLM_BATCH_SIZE=25
LLM_TIMEOUT=120
